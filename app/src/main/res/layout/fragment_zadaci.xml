<?xml version="1.0" encoding="utf-8"?>
<!-- fragment_zadaci.xml -->
<!-- Sadrzi: RecyclerView (lista zadataka) + FloatingActionButton (plavo) -->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- RECYCLERVIEW - prikazuje listu zadataka -->
    <!-- Adapter se postavlja u ZadaciFragment.java -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"
        android:clipToPadding="false" />

    <!-- FLOATING ACTION BUTTON - dugme za dodavanje novog zadatka -->
    <!-- Pocetna boja: plava. Menja se u crvenu ako je neparan broj zadataka -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:contentDescription="Dodaj zadatak"
        app:backgroundTint="#2196F3"
        app:srcCompat="@android:drawable/ic_input_add" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
