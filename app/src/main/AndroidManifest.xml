<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kolokvijum1">

    <!-- Dozvola za notifikacije (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kolokvijum1">

        <!-- Glavna aktivnost -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Servis koji proverava boju svakih minut -->
        <service
            android:name=".ProveraServis"
            android:enabled="true"
            android:exported="false" />

        <!-- BroadcastReceiver za dodavanje zadataka -->
        <receiver
            android:name=".ZadatakReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="ZADATAK_DODAT" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
